{%extends "base.jinja"%}
{% block head %}
<title>Vote!</title>
{% endblock %}

{% block body %}
<h1>Pick your Favourite Scripts!</h1>
{% if message != " "%}
<h3>{{message}}</h3>
{% endif %}

<form action="/vote" method="post">
    <label for="name">Name:</label> <input type="text" name="name" placeholder="Your Name" value="{{ name }}" required><br>
    <label for="filter">Search:</label> <input type="text" name="filter" placeholder="Search..." oninput="filter_options(this.value);"><br><br>

    {% for script_name in available_scripts %}
    <label name="script" value="{{ script_name }}">
        <input type="checkbox" name="script_checkbox" value="{{ script_name }}" {% if script_name in current_votes %}checked{% endif %}> {{ script_name }}<br>
    </label>
    {% endfor %}
    <br>

    <input type="submit" value="Cast Vote!">
</form>

<h3>See the results <a href="/results">here!</a></h3>
<script src="/static/js/filter.js"></script>
{% endblock %}
